import React from 'react';
import styles from '../footer/footer.module.css';

const Footer = () => (
  <footer className={styles.footer}>
    <p className={styles.title}>Add a card!</p>
  </footer>
);
export default Footer;
